---
# defaults file for common

docker_dependencies:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - software-properties-common

default_packages:
  - ntp 
  - docker-ce 
  - containerd.io
  - bridge-utils 
  - cgroupfs-mount  
  - pigz 
  - ubuntu-fan
  - python3-pip

pip_packages:
  - docker